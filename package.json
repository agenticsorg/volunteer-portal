{
  "name": "volunteer-portal",
  "version": "0.0.0",
  "private": true,
  "description": "Agentics Foundation Volunteer Portal — gamified, social volunteer-management platform with a training-video library.",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test:unit": "pnpm --filter web test:unit",
    "test:integration": "dotenv -e .env -- pnpm --filter web test:integration",
    "e2e:smoke": "pnpm --filter web e2e:smoke",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:generate": "pnpm --filter web exec prisma generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter web exec prisma migrate dev",
    "db:migrate:deploy": "dotenv -e .env -- pnpm --filter web exec prisma migrate deploy",
    "db:studio": "dotenv -e .env -- pnpm --filter web exec prisma studio",
    "worker:dev": "dotenv -e .env -- pnpm --filter worker dev",
    "worker:start": "dotenv -e .env -- pnpm --filter worker start",
    "outbox:example": "dotenv -e .env -- pnpm --filter @volunteer-portal/outbox example"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.32.1"
}
