{
  "name": "volunteer-portal",
  "version": "0.0.0",
  "private": true,
  "description": "Agentics Foundation Volunteer Portal — gamified, social volunteer-management platform with a training-video library.",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "lint:boundaries": "pnpm --filter web lint:boundaries"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.32.1"
}
