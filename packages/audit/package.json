{
  "name": "@volunteer-portal/audit",
  "version": "0.0.0",
  "private": true,
  "description": "Shared recordAuditEvent() helper every privileged action funnels through (ADR-0014 §4): writes an 'audit.recorded' event to the caller's own schema's domain_events outbox, inside the caller's own transaction.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@volunteer-portal/ulid": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
