{
  "name": "@volunteer-portal/authz",
  "version": "0.0.0",
  "private": true,
  "description": "Shared can(subject, action, resource) scoped-RBAC policy module (ADR-0007) every bounded-context module imports for authorization decisions. Reads identity.role_assignments facts the caller supplies — has zero database/HTTP dependencies of its own.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
