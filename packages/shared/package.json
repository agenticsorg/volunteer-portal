{
  "name": "@volunteer-portal/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Small, dependency-free application-config tables shared by multiple bounded-context modules — currently just notifications' per-type channel/delivery-mode defaults (docs/ddd/notifications.md) — so this config can change without touching handler code and without any module reaching into another module's internals for it.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
