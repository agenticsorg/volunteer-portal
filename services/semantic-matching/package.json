{
  "name": "semantic-matching",
  "version": "0.1.0",
  "private": true,
  "description": "Prompt 9.1 (ADR-0013): the isolated ruvector-based semantic matching service. Read-only, no database access, no authorization-sensitive state -- see .plans/adrs/0013-semantic-matching-vector-layer.md.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ruvector": "^0.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
